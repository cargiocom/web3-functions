const UrlBuilder = {
    log: (froms: string[], tos: string[], values: number[], datas: string[], chainids: number[]) => {
        for (let i = 0; i < froms.length; i++) {
            const from = froms[i];
            const to = tos[i];
            const value = values[i];
            const data = datas[i];
            const chainid = chainids[i];4444
        }
    }
};
export { UrlBuilder };
